# v0.1.0 Public GitHub Pages Smoke Report

- Date/time: 2026-05-02 07:04:41 +02:00
- App commit under test: `1ec17f68d46bbe11ee83b025d3e51a32b6f3795d`
- Workflow run id: `25244305268`
- Public URL: https://ommmmm88888.github.io/b1/

## GitHub Pages Deploy Status

- Pages source: GitHub Actions
- Deploy workflow status: PASS (`completed/success`)
- Pages API: `html_url=https://ommmmm88888.github.io/b1/`, `build_type=workflow`, `source.branch=main`, `source.path=/`, `public=true`, `https_enforced=true`
- Public URL HTTP result: PASS (`200`)

## Public Smoke Checklist

1. Public URL loads: PASS (`200`)
2. Russian UI is visible: PASS
3. Header account/login control is visible: PASS (`Google вход`)
4. Firebase safe state is visible when Firebase is not configured: PASS (`не настроено`)
5. No blank screen: PASS
6. `manifest.webmanifest` is reachable: PASS (`200`)
7. `sw.js` is reachable: PASS (`200`)

## Public Functional Smoke

Modes opened on public URL:

- `Тренировка`: PASS
- `Интенсив`: PASS
- `Грамматика`: PASS
- `Письмо`: PASS
- `Говорение`: PASS
- `Чтение`: PASS
- `Аудирование`: PASS
- `Пробный экзамен`: PASS

Interactions:

- Trainer text input and `Проверить` action: PASS
- Feedback after answer check: PASS (`Ответ неверный`, `Правильный ответ`, `Ваш ответ`)
- LocalStorage persistence after reload: PASS

## Account / Sync UI

- Firebase config source: `VITE_FIREBASE_*` environment variables.
- Public deployment state: Firebase not configured.
- Header control result: PASS. The app shows `Google вход` with `не настроено` instead of crashing.
- No private Firebase secrets were found in the release audit. The repository contains placeholders in `.env.example`, a Firebase project id in `.firebaserc`, and Firestore security rules.

## PWA / Offline

- Service worker registered: PASS
- Service worker scope contains `/b1/`: PASS
- Cache Storage contains app cache: PASS (`b1-precache-v1-2026-04-27`)
- Offline reload: PASS (headless Chromium offline mode reload kept the app shell available)

## Limitations

- No backend is included in this release.
- Firebase account sync remains disabled on the public deployment until repository environment variables and Firebase project settings are configured.
- Writing and speaking use self-check, not AI evaluation.
- Listening uses browser SpeechSynthesis, not human-recorded exam audio.
- Mini mock exam is intentionally short and is not a full official-length mock.

## Issues Found

- None during this public smoke run.

## Fixes Made

- No code fixes required.

## Result

- Public smoke result: PASS
- Account/sync UI result: PASS safe disabled state
- PWA/offline result: PASS
- Recommendation: **ready for release tag** after this smoke report docs commit is verified by GitHub Pages and the working tree is clean.
