# v0.1.0 RC Smoke Report

## Date / Commit

- Date/time: 2026-05-01 07:34:35 +02:00
- Start commit: `d7e6398`
- Fix commit created during smoke: `64fbe58`
- Branch: `main`
- Local preview URL: `http://127.0.0.1:4173/b1/`

## Commands Run

```powershell
Set-Location F:\repo\b1
git status --short --branch
git log --oneline -5
Get-Content package.json -Raw
Get-Content docs\releases\v0.1.0-rc.md -Raw
npm run verify
npm run build
npx playwright test --config results\playwright-smoke.config.ts --reporter=line
node -e "<targeted Playwright browser checks>"
npm run verify
git status --short
git commit -m "Fix RC smoke event handling issues"
```

Production preview was started with:

```powershell
Start-Process -FilePath 'pwsh' -ArgumentList @('-NoProfile','-Command','Set-Location F:\repo\b1; npm run preview -- --host 127.0.0.1 --port 4173') -WindowStyle Hidden
```

Observed preview output:

```text
Local: http://127.0.0.1:4173/b1/
```

## Checklist

- App shell: PASS. App loaded, title `B1 Polish Trainer`, full Russian navigation visible.
- Vocabulary trainer: PASS. Correct answer, incorrect answer, explanation path and reload persistence were exercised before the first blocker.
- 12-day intensive: PASS. 12-day mode opened, day selector worked, checklist changed, note saved, reset confirmation appeared, summary block was visible.
- 8-week intensive: PASS. 8-week mode opened, week selector worked, selected week persisted after reload.
- Grammar: PASS. Topic selection, answer checking, explanation and common mistake display worked.
- Writing: PASS. Type/task selection, draft entry, save button and self-check checklist worked.
- Speaking: PASS. Type selection, timer start/pause and completion marking worked.
- Reading: PASS after fix. Questions can be answered, check button enables, result/explanations appear, no page errors.
- Listening: PASS after fix. SpeechSynthesis was available in Chromium, play/stop worked, questions and shadowing checkbox worked, result appeared.
- Mini mock: PASS after fix. Objective parts and self-checks completed, readiness/weak zones displayed, latest result persisted after reload.
- PWA/offline basics: PARTIAL. `manifest.webmanifest` and `sw.js` are reachable; asset paths use `/b1/`. Headless Chromium did not confirm service worker registration within the smoke window, so DevTools/Application manual confirmation is still recommended before public release.

## Issues Found

1. Release blocker: reading radio answers could blank the app with `Cannot read properties of null (reading 'value')`.
2. Release blocker: mini mock self-check checkboxes could blank the app with the same React synthetic event lifetime issue.
3. Same risky event pattern existed in trainer and grammar/listening handlers.

## Fixes Made

- Captured input/radio values before functional state updaters.
- Captured checkbox `checked` before functional state updaters in mini mock self-check.
- Rebuilt production assets and repeated affected browser checks.

## Proof After Fix

```powershell
npm run verify
```

Result:

- lint passed;
- tests passed: 4 files, 11 tests;
- build passed.

Targeted browser checks after fix:

- reading: PASS, no page errors;
- listening: PASS, SpeechSynthesis available, no page errors;
- mini mock: PASS, no page errors.

## Final Recommendation

Ready for push after one optional manual browser DevTools check of service worker registration. Do not tag or deploy until push/release is explicitly approved.

## PWA / Service Worker Follow-Up

- Date/time: 2026-05-01 07:48:51 +02:00
- Local preview URL: `http://127.0.0.1:4173/b1/`
- Manifest result: PASS. `http://127.0.0.1:4173/b1/manifest.webmanifest` returned OK.
- `sw.js` result: PASS. `http://127.0.0.1:4173/b1/sw.js` returned OK with JavaScript content.
- Asset path result: PASS. Production HTML loaded favicon, manifest, JS and CSS from `/b1/`.
- Registration result: PASS after release fix. Headless Chromium reported service worker registration scope `http://127.0.0.1:4173/b1/`, `controller: true`, and cache key `b1-precache-v1-2026-04-27`.
- Offline result: PASS. After service worker activation, Chromium `context.setOffline(true)` still loaded `http://127.0.0.1:4173/b1/` and showed `B1 Polish Trainer`.
- Preview server stopped: PASS. Port `4173` had no listening process after shutdown; only transient `TimeWait` entries remained.
- Final recommendation: ready for `git push origin main`. Do not create a tag yet.

Fix made during this follow-up:

- Added a production `/b1/` inline service worker registration fallback in `index.html`.
- Kept the existing TypeScript registration helper in place.
