# v0.1.0 RC Smoke Report

## Date / Commit

- Date/time: 2026-05-01 07:34:35 +02:00
- Branch: `main`
- Local preview URL: `http://127.0.0.1:4173/b1/`

## Commands Run

```powershell
Set-Location F:\repo\b1
git status --short --branch
git log --oneline -5
npm run verify
npm run build
npx playwright test --config results\playwright-smoke.config.ts --reporter=line
node -e "<targeted Playwright browser checks>"
npm run verify
```

Production preview was started with:

```powershell
npm run preview -- --host 127.0.0.1 --port 4173
```

Observed preview output:

```text
Local: http://127.0.0.1:4173/b1/
```

## Checklist

- App shell: PASS. App loaded, title `B1 Polish Trainer`, full Russian navigation visible.
- Vocabulary trainer: PASS. Correct answer, incorrect answer, explanation path and reload persistence were exercised.
- 12-day intensive: PASS. 12-day mode opened, day selector worked, checklist changed, note saved, reset confirmation appeared, summary block was visible.
- Grammar: PASS. Topic selection, answer checking, explanation and common mistake display worked.
- Writing: PASS. Type/task selection, draft entry, save button and self-check checklist worked.
- Speaking: PASS. Type selection, timer start/pause and completion marking worked.
- Reading: PASS. Questions can be answered, check button enables, result/explanations appear, no page errors.
- Listening: PASS. SpeechSynthesis was available in Chromium, play/stop worked, questions and shadowing checkbox worked, result appeared.
- Mini mock: PASS. Objective parts and self-checks completed, readiness/weak zones displayed, latest result persisted after reload.
- PWA/offline basics: PASS after follow-up. `manifest.webmanifest` and `sw.js` are reachable; asset paths use `/b1/`; service worker registration and offline reload were confirmed.

## Issues Found And Fixed During RC

1. Reading radio answers could blank the app with `Cannot read properties of null (reading 'value')`.
2. Mini mock self-check checkboxes could blank the app with the same React synthetic event lifetime issue.
3. Same risky event pattern existed in trainer and grammar/listening handlers.

## Fixes Made

- Captured input/radio values before functional state updaters.
- Captured checkbox `checked` before functional state updaters in mini mock self-check.
- Rebuilt production assets and repeated affected browser checks.

## Proof After Fix

```powershell
npm run verify
```

Result:

- lint passed;
- tests passed;
- build passed.

Targeted browser checks after fix:

- reading: PASS, no page errors;
- listening: PASS, SpeechSynthesis available, no page errors;
- mini mock: PASS, no page errors.

## PWA / Service Worker Follow-Up

- Manifest result: PASS. `http://127.0.0.1:4173/b1/manifest.webmanifest` returned OK.
- `sw.js` result: PASS. `http://127.0.0.1:4173/b1/sw.js` returned OK with JavaScript content.
- Asset path result: PASS. Production HTML loaded favicon, manifest, JS and CSS from `/b1/`.
- Registration result: PASS. Headless Chromium reported service worker registration scope `http://127.0.0.1:4173/b1/`, `controller: true`, and cache key `b1-precache-v1-2026-04-27`.
- Offline result: PASS. After service worker activation, Chromium offline mode still loaded `http://127.0.0.1:4173/b1/` and showed `B1 Polish Trainer`.

## Final Recommendation

Ready for release tag after final release-prep docs/version commit is verified.
